---
name: replace-with-skill-name
description: Replace with a precise capability and when it should trigger. Include a boundary when a nearby task should not trigger it.
---

# Replace with skill title

State the outcome and the non-obvious constraints. Keep this entrypoint short.

## Workflow

Describe only the decisions and steps another agent needs to perform reliably.

## Resources

- Put conditional detail in `references/` and link it here with when to read it.
- Put repeated deterministic transformations in `scripts/` and test them.
- Add `assets/` only for files copied into outputs.

Remove all scaffold text before activating or validating the skill.
